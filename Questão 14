#include <stdio.h> // biblioteca padrão

int main() { // função principal
    int num, i; // variáveis

    printf("Digite um numero: "); // pede número
    scanf("%d", &num); // lê número

    for (i = 1; i <= 10; i++) { // loop de 1 a 10
        printf("%d x %d = %d\n", num, i, num * i); // mostra tabuada
    }

    return 0; // fim
}
